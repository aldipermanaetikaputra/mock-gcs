import MockStorage, { IStorage } from './MockStorage';
import MockBucket, { IBucket } from './MockBucket';
import MockFile, { IFile } from './MockFile';

export { MockStorage, MockBucket, MockFile };
export { IStorage, IBucket, IFile };
